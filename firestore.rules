rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all reads and writes for data migration
    // TODO: Restrict permissions after migration is complete
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
